<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ProductMapper">

    <resultMap id="productResultMap" type="com.example.entity.Product">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
    </resultMap>

    <!-- MySQL style: SELECT LAST_INSERT_ID() AFTER insert -->
    <insert id="insertMysql" parameterType="com.example.entity.Product">
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO products (name, price) VALUES (#{name}, #{price})
    </insert>

    <!-- Oracle style: SELECT seq.NEXTVAL BEFORE insert -->
    <insert id="insertOracle" parameterType="com.example.entity.Product">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT seq_product.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO products (id, name, price) VALUES (#{id}, #{name}, #{price})
    </insert>

    <!-- useGeneratedKeys style (already handled) -->
    <insert id="insertPg" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, price) VALUES (#{name}, #{price})
    </insert>
</mapper>
